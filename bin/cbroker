#!/usr/bin/env node
import('../dist/cli/index.js').catch((err) => {
  console.error('cbroker: failed to start —', err?.message ?? err);
  console.error('Did you run `npm run build`?');
  process.exit(1);
});
